# Enterprise AI Chat Frontend

Run:
1. Open this folder in VS Code.
2. Terminal: `npm install`
3. Terminal: `npm run dev`
4. Open http://localhost:3000

This is a frontend-only chat page. The AI answer is currently a demo response.
Later, replace the demo `setTimeout` in `app/page.js` with your backend/RAG API.
